process L1DTTrigTreeGen = { 

	include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
	include "Geometry/DTGeometry/data/dtGeometry.cfi"
	include "L1TriggerConfig/DTTPGConfigProducers/data/L1DTConfig.cfi"

  source = EmptySource {
    untracked int32 maxEvents = 1
  }

  es_source rcdsrc = EmptyESSource {
    string recordName = "DTConfigManagerRcd"
    vuint32 firstValid = { 1 }
    bool iovIsRunNotTime = false
  }

  module esgetdata = EventSetupRecordDataGetter {
    untracked bool verbose = true
    VPSet toGet = {
        {
          string record = "DTConfigManagerRcd"
          vstring data = { "DTConfigManager" }
        }
    }
  }

  source = PoolSource{ 
       	untracked vstring fileNames = {"file:digi.root"} 
  }

  untracked PSet maxEvents = {untracked int32 input = -1}

	include "L1Trigger/DTTrigger/data/dttriganalyzer.cfi"

  path p = {dttriganalyzer}
}
